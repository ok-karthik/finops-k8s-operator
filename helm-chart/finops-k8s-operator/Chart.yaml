apiVersion: v2
name: finops-k8s-operator
description: A Helm chart for the FinOps Sleep/Wake Kubernetes operator
type: application
version: 0.7.0
# appVersion is purely informational; tie it to the image tag so that `helm list`
# shows the actual operator version that will run.
appVersion: "{{ .Values.image.tag }}"
